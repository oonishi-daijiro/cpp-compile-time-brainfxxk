#pragma once

// brainfxxk operations
struct inc;  // +
struct dec;  // -
struct iptr; // >
struct dptr; // <
struct out;  //.
struct in;   //
struct lb;   //[
struct le;   // ]

struct end; // termination(not brainfuck operation)
